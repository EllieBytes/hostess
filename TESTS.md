# Testing.

Hostess is currently still under testing.

It can do the following:

+ [ ] Build a host.
+ [ ] Build a user.
+ [ ] Resolve a module at a directory ("extra/testmod" -> ./modules/extra/testmod/default.nix v.s ./modules/extra/testmod.nix)
+ [ ] Build my full setup (5 hosts, 1 overlay, many modules, images).
